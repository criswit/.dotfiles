# AWS Credentials Template
# This file will be deployed to ~/.aws/credentials when using stow
# 
# IMPORTANT: This is a template file. You should:
# 1. Never commit actual credentials to version control
# 2. Use aws-vault or similar credential managers for production
# 3. Replace placeholder values with your actual configuration
#
# For secure credential management, consider using:
# - aws-vault (recommended)
# - AWS SSO
# - IAM roles for EC2 instances
# - Environment variables

[default]
# Default profile credentials
# aws_access_key_id = YOUR_ACCESS_KEY_ID
# aws_secret_access_key = YOUR_SECRET_ACCESS_KEY
# aws_session_token = 

[dev]
# Development profile credentials
# aws_access_key_id = DEV_ACCESS_KEY_ID
# aws_secret_access_key = DEV_SECRET_ACCESS_KEY
# aws_session_token = 

[staging]
# Staging profile credentials
# aws_access_key_id = STAGING_ACCESS_KEY_ID
# aws_secret_access_key = STAGING_SECRET_ACCESS_KEY
# aws_session_token = 

[prod]
# Production profile credentials
# aws_access_key_id = PROD_ACCESS_KEY_ID
# aws_secret_access_key = PROD_SECRET_ACCESS_KEY
# aws_session_token = 

# Note: For production environments, consider using:
# - IAM roles instead of access keys
# - AWS SSO for temporary credentials
# - aws-vault for secure credential storage
# - Environment variables for CI/CD pipelines
